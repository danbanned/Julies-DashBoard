// Phase 12a: the hardcoded single-user id is retired — routes now use the
// authenticated session (lib/session.js). This constant remains only for
// scripts/seeds that target Julie's admin row directly.
export const CURRENT_USER_ID = "julie";
